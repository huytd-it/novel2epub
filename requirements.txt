ebooklib>=0.18
PyYAML>=6.0.1
ruamel.yaml>=0.18.0
fastapi>=0.110.0
uvicorn>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9
pytest>=8.0.0
croniter>=2.0
PyJWT[crypto]>=2.8.0
httpx>=0.27.0

# Đọc thành tiếng trên trang đọc (/api/tts) — giọng Edge Neural
edge-tts>=7.0

# Engine crawl: Scrapling (anti-bot bypass, stealth browser)
# pip install scrapling[fetchers] && scrapling install
scrapling[fetchers]>=0.2

# Optional: dùng crawl.scrapling.proxy dạng socks5:// — requests (tải ảnh bìa)
# cần PySocks: pip install "requests[socks]" (Scrapling/curl_cffi không cần)
# requests[socks]

# Optional: backend dịch cục bộ HachimiMT/MoxhiMT (translate.type=hachimimt)
# pip install ctranslate2 sentencepiece huggingface_hub
# ctranslate2>=4.0.0
# sentencepiece>=0.2.0
# huggingface_hub>=0.23.0
