ebooklib>=0.18
PyYAML>=6.0.1
ruamel.yaml>=0.18.0
fastapi>=0.110.0
uvicorn>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9
pytest>=8.0.0
croniter>=2.0
PyJWT[crypto]>=2.8.0
httpx>=0.27.0

# Text-to-speech tren trang doc (/api/tts) - giong Edge Neural
edge-tts>=7.0

# Engine crawl: Scrapling (anti-bot bypass, stealth browser)
# pip install scrapling[fetchers] && scrapling install
scrapling[fetchers]>=0.2

# Optional: dung crawl.scrapling.proxy dang socks5:// - requests (tai anh bia)
# can PySocks: pip install "requests[socks]" (Scrapling/curl_cffi khong can)
# requests[socks]

# Optional: backend dich cuc bo HachimiMT/MoxhiMT (translate.type=hachimimt)
# pip install ctranslate2 sentencepiece huggingface_hub
# ctranslate2>=4.0.0
# sentencepiece>=0.2.0
# huggingface_hub>=0.23.0
